repos:
  - repo: https://github.com/realm/SwiftLint
    rev: 0.63.2
    hooks:
      - id: swiftlint
        name: swiftlint-format
        entry: swiftlint --fix --format --force-exclude
      - id: swiftlint
        name: swiftlint-strict
        entry: swiftlint --strict --force-exclude
